[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
flask = ">=2.3,<3.0"
flask-wtf = ">=1.1,<2.0"
flask-mail = ">=0.9.1,<1.0"
gunicorn = ">=23.0,<24.0"

[dev-packages]
black = ">=25.1,<26.0"
ruff = ">=0.12,<1.0"
pytest = ">=8.4,<9.0"
pytest-flask = ">=1.3,<3.0"

[requires]
python_version = "3.14"
